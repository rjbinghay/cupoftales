{
  "name": "storysip-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "Tailwind build pipeline for StorySip. Source: static/css/input.css → built: static/css/main.css",
  "scripts": {
    "//": "Run `npm run dev` while developing (rebuilds on save). Run `npm run build` before deploy.",
    "dev": "tailwindcss -i ./static/css/input.css -o ./static/css/main.css --watch",
    "build": "tailwindcss -i ./static/css/input.css -o ./static/css/main.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "@tailwindcss/typography": "^0.5.16",
    "tailwindcss": "^4.0.0"
  },
  "dependencies": {
    "anthropic": "^0.0.0"
  }
}
